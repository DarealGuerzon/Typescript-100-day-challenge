// Week 2 Milestone Challenge — User Role System
// Task: Build a typed user system with roles and optional fields.
// 1. Create a User type with name, age, role ('admin' | 'user'), and optional email
// 2. Create an enum for Role instead of union type
// 3. Write createUser with typed parameters and explicit return type
// 4. Create a User[] array with 3 users (mix of admins and users, some with email)
// 5. Print each user's info, showing "No email" if email is missing
// Rules: TypeScript only, ❌ any, use enum, optional props, return types
// Done When: Compiles, prints all users with roles and email handling
